.search-bar
  - form_tag search_path, {:method => :get} do |f|
    %label{:for => 'q'}
      Search
    = text_field_tag 'q', (@keywords.join(' ') if @keywords), :id => 'q', :class => 'search-field', :size => '30'
    = hidden_field_tag 'city', City.current.name.parameterize if City.current
