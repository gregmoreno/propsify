#setup.span-16.rounded
  .box
    .header
      %h1.title
        Update a clinic location

    = flash_error

    - form_for object, :url => user_location_path  do |f|
      = render :partial => 'form', :locals => {:f => f} 
      %p
        = submit_tag 'Save changes', :class => "submit-button"

